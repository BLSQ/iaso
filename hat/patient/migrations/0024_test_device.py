# Generated by Django 2.0 on 2019-02-08 15:14

from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    dependencies = [
        ('sync', '0023_ptr'),
        ('patient', '0023_hide_tests'),
    ]

    operations = [
        migrations.AddField(
            model_name='test',
            name='device',
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.CASCADE, to='sync.DeviceDB'),
        ),
        migrations.RunSQL(sql="""
            update patient_test pt set device_id=ddb.id
            from sync_jsondocument jd inner join sync_devicedb ddb
              on jd.doc->'participant'->'screenings'->'rdt'->>'device' = ddb.device_id
            where jd.case_id is not null
            and jd.doc->'participant'->'screenings'->'rdt'->'device' is not null
            and pt.form_id=jd.case_id
            and pt.type='RDT'
        """, reverse_sql=""),
        migrations.RunSQL(sql="""
            update patient_test pt set device_id=ddb.id
            from sync_jsondocument jd inner join sync_devicedb ddb
              on jd.doc->'participant'->'screenings'->'catt'->>'device' = ddb.device_id
            where jd.case_id is not null
            and jd.doc->'participant'->'screenings'->'catt'->'device' is not null
            and pt.form_id=jd.case_id
            and pt.type='CATT'
        """, reverse_sql=""),
        migrations.RunSQL(sql="""
            update patient_test pt set device_id=ddb.id
            from sync_jsondocument jd inner join sync_devicedb ddb
              on jd.doc->'participant'->'screenings'->'maect'->>'device' = ddb.device_id
            where jd.case_id is not null
            and jd.doc->'participant'->'screenings'->'maect'->'device' is not null
            and pt.form_id=jd.case_id
            and pt.type='MAECT'
        """, reverse_sql=""),
        migrations.RunSQL(sql="""
            update patient_test pt set device_id=ddb.id
            from sync_jsondocument jd inner join sync_devicedb ddb
              on jd.doc->'participant'->'screenings'->'pg'->>'device' = ddb.device_id
            where jd.case_id is not null
            and jd.doc->'participant'->'screenings'->'pg'->'device' is not null
            and pt.form_id=jd.case_id
            and pt.type='PG'
        """, reverse_sql=""),
        migrations.RunSQL(sql="""
            update patient_test pt set device_id=ddb.id
            from sync_jsondocument jd inner join sync_devicedb ddb
              on jd.doc->'participant'->'screenings'->'pl'->>'device' = ddb.device_id
            where jd.case_id is not null
            and jd.doc->'participant'->'screenings'->'pl'->'device' is not null
            and pt.form_id=jd.case_id
            and pt.type='PL'
        """, reverse_sql=""),
    ]
