name: List_configs
on:
    pull_request:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
     - uses: actions/checkout@v3
     - name: "Test printing PR body"
       run: .scripts/list_config_changes.py ${{github.event.pull_request.body}}
