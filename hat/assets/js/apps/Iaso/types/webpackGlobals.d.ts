/** Production: federation remoteEntry URL (webpack.prod.js). */
declare const __ODK_PREVIEW_REMOTE_ENTRY__: string;

/** Development: Vite serves src/mount.ts directly (webpack.dev.js). */
declare const __ODK_PREVIEW_DEV_MOUNT__: string;

/** True in webpack dev only. */
declare const __ODK_PREVIEW_DEV__: boolean;
