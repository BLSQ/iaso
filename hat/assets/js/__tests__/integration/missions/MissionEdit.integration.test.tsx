describe('Mission edit integration test', () => {
    it('renders loading state', () => {
        expect(false).toBeTruthy();
    });

    it('renders 404 if data is not found', () => {
        expect(false).toBeTruthy();
    });

    it('renders initial data - MISSION_FORM', () => {
        expect(false).toBeTruthy();
    });
    it('renders initial data - MISSION_ENTITY_TYPE', () => {
        expect(false).toBeTruthy();
    });
    it('renders initial data - MISSION_ORG_UNIT', () => {
        expect(false).toBeTruthy();
    });

    it('handles forms uniqueness', () => {
        expect(false).toBeTruthy();
    });

    it('does not call API if form is invalid - MISSION_FORM', () => {
        expect(false).toBeTruthy();
    });
    it('does not call API if form is invalid - MISSION_ENTITY_TYPE', () => {
        expect(false).toBeTruthy();
    });
    it('does not call API if form is invalid - MISSION_ORG_UNIT', () => {
        expect(false).toBeTruthy();
    });

    it('displays errors from backend - MISSION_FORM', () => {
        expect(false).toBeTruthy();
    });
    it('displays errors from backend - MISSION_ENTITY_TYPE', () => {
        expect(false).toBeTruthy();
    });
    it('displays errors from backend - MISSION_ORG_UNIT', () => {
        expect(false).toBeTruthy();
    });

    it('submits the data and redirect - MISSION_FORM', () => {
        expect(false).toBeTruthy();
    });
    it('submits the data and redirect - MISSION_ENTITY_TYPE', () => {
        expect(false).toBeTruthy();
    });
    it('submits the data and redirect - MISSION_ORG_UNIT', () => {
        expect(false).toBeTruthy();
    });

    it('redirects to detail view if cancel button is clicked', () => {
        expect(false).toBeTruthy();
    });
});
